package com.cpu8086.model;

public enum RegisterType {
    // General purpose registers
    AX, BX, CX, DX,
    // Index registers
    SI, DI,
    // Pointer registers
    BP, SP,
    // Segment registers
    CS, DS, SS, ES,
    // Instruction pointer
    IP
}

